<%= form_with(model: bottle_type) do |form| %>
  <% if bottle_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bottle_type.errors.count, "error") %> prohibited this bottle_type from being saved:</h2>

      <ul>
        <% bottle_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
